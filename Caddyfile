# Caddy auto-provisions & renews a free Let's Encrypt certificate for $DOMAIN.
# No manual certbot, no nginx tuning. Set DOMAIN + ACME_EMAIL in .env.production.
{
	email {$ACME_EMAIL}
}

{$DOMAIN} {
	encode gzip zstd
	reverse_proxy app:3000
}
